{
  "buildCommand": "npm run build && npm run build:api",
  "outputDirectory": "dist/dnd-grimoire/browser",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
